# Sprint 2026-W24 (6/8 – 6/12)

## Sprint Goal
基础设施升级起步 (S2.x 评估与规划) + 人物关系管理前端 UI

## Completed (Previous Sprint)

### v0.4.0 安全加固 — 全部完成 ✅
- S1.1 Helmet CSP 生产/开发双配置
- S1.2 Socket.IO JWT 认证中间件
- S1.3 CORS 白名单机制（`CORS_ORIGINS` 环境变量）
- S1.4 全局速率限制（100/min prod, 500/min dev）+ auth 专用限制（10/15min）
- S1.5 密码策略强化（Zod: min 8, 大小写+数字, bcrypt 12 rounds）
- S1.6 optionalAuthenticate 错误处理（已正确实现空 catch 静默跳过）
- S1.7 注册 role 参数注入防护（移除 role 字段，硬编码 'reader'）
- S1.8 Profile JSON 结构验证（新增 profileSchema）

### S3.1 人物关系管理 — 后端 API ✅
- `characterController.ts` — getCharacterAppearances, create, delete
- `routes/characters.ts` — GET/POST/DELETE 路由
- `routes/stories.ts` — getStoryCharacterAppearances, batchCharacterAppearances

## In Sprint

| Task | Story | Priority | Owner | Status |
|------|-------|----------|-------|--------|
| SQLite → PostgreSQL 迁移评估 | S2.1 | P1 | TBD | ⬜ |
| API 响应格式统一（确定规范 + 批量修改） | S2.4 | P1 | TBD | ✅ |

## Completed
- S3.1 人物关系管理 — 后端 API + 前端 UI (CharacterManager + AppearanceManager 已集成到 MainlinePage)
- v0.4.0 全部安全加固 S1.1–S1.8

## Not In Sprint
- JWT refresh token (S2.3) → W25
- JSON-as-String → JSON 字段 (S2.2) → 跟随 PG 迁移
- 阅读路径编辑页修复 (S3.2) → ✅
- 表单验证库 (S3.3) → v0.6.0

## Notes
- Husky pre-commit hook updated: now runs `tsc --noEmit` for frontend + backend instead of broken `lint-staged`
- `--no-verify` no longer needed for commits
