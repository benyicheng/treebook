# 2026-03-26 工作日志（第四部分）

## 首页热门书单功能实现

**需求**：把项目书单功能在首页热门书单展示，注意交互。

**实施内容**：

### 1. 后端改进
`api/src/controllers/booklistController.ts` 的 `getBooklists` 函数：
- 增加 `limit` 参数：限制返回数量
- 增加 `sortBy` 参数：支持按 `viewCount` 热度排序或 `createdAt` 创建时间排序
- 默认按 `createdAt` 降序

### 2. 前端集成
`src/pages/Home.tsx`：
- 导入 `booklistService`
- 添加 `hotBooklists` 状态
- 添加 `fetchHotBooklists` 函数：获取前 3 个书单
- 在 `useEffect` 中调用 `fetchHotBooklists`
- 替换「热门书单」区块硬编码数据为真实 API 数据

### 3. 交互设计
- **卡片布局**：3 列网格，展示书单封面、标题、作者、章节数、浏览量
- **视觉效果**：hover 阴影放大，渐变遮罩
- **标签**：右上角「热门」标签
- **空状态**：无书单时显示书本图标和提示文字

**影响范围**：
- `api/src/controllers/booklistController.ts`
- `src/pages/Home.tsx`

**验证**：
- ✅ 后端编译通过
- ✅ 前端 lint 无错误
- ✅ 项目正常启动
